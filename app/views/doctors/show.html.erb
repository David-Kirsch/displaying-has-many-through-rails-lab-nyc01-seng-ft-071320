<h1><%= @doctor.name %></h1>
<h4><%= @doctor.department %></h4>
<p>
<h2>Appointments:</h2>
<% @doctor.appointments.each do |appt|%>
    <%= appt.appointment_datetime.strftime("%B %d, %Y at %l:%M%p")%>
   - <%= link_to appt.patient.name, patient_path(appt.patient) %><br>
    <% end%>
</p>