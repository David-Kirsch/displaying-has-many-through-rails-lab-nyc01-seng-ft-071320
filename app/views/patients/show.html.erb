<h1><%= @patient.name %></h1>

<h2>Appointments:</h2>
<% @patient.appointments.each do |appt|%>
    <%= link_to appt.appointment_datetime.strftime("%B %d, %Y at %l:%M%p"), appointment_path(appt) %>
    - <%= link_to appt.doctor.name, doctor_path(appt.doctor) %><br>
    <% end%>